import 'package:flutter/material.dart';
class SuccessfulLoginDashboard extends StatelessWidget {
  const SuccessfulLoginDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Login successful'),
    );
  }
}
